import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#A73B35'
    },
    secondary: {
      main: '#0982E3'
    },
    background: {
      default: '#F3F3F3'
    }
  }
});

export default theme;