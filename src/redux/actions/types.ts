/* eslint-disable no-unused-vars */

/**
 * @enum All action types that can be used for Redux store updates.
 */
enum ActionTypes {
    fetchProjectDetailSuccess = 'fetchProjectDetailSuccess',
    fetchUserListSuccess = 'fetchUserListSuccess'
}

export default ActionTypes;