/**
 * @enum Role enum definition for selecting role of particular user.
 */

/* eslint no-unused-vars: "off" */
enum RoleEnum {
    Admin = 'admin',
    Customer = 'customer',
    Manager = 'manager'
}
export default RoleEnum;