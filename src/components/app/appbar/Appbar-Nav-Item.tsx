import { Typography } from '@material-ui/core';
import styled from 'styled-components';


const AppbarNavitem = styled(Typography)`
  flex-grow: 1;
  cursor: pointer;
`;

export default AppbarNavitem;
